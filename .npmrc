# @pedrolucaslopes/dotlog-ui mora no GitHub Packages, que exige token ate para instalar.
# O token vem do ambiente (NODE_AUTH_TOKEN, com read:packages) e nunca entra neste arquivo.
@pedrolucaslopes:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
